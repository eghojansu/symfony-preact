import Router from 'preact-router'
import Panel from '@app/components/panel'
import HomePage from './home'

export default ({ path }) => {
  return (
    <Panel title="Account">
      Account Page
    </Panel>
  )
}